"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/* class Player {
    // puplic attributes
    private name: string;
    private health: number;
    private speed: number;
    private crush: Player | null;
    constructor(
            healthVal: number = 0,
            speedVal: number = 0,
            crush:Player | null = null,
            nameVal: string
        ) {
        this.health = healthVal;
        this.name = nameVal;
        this.speed = speedVal;
        this.crush = crush;
    }

    // public method
    greet() {
        console.log(`Hello I am a ${this.name}!`)
    }

    setCrushOn(playerObj: Player | null) {
        this.crush = playerObj;
    }

    getCrushOn() {
        return this.crush;
    }

    displayCrush() {
        console.log(`Hey I am a ${this.name} and i crush on ${this.crush?.name ?? "No one :("}`)
    }
}

const mario = new Player(20, 10, null, "mario");
const peach = new Player(10, 5, mario, "peach");

mario.greet();
mario.displayCrush();

peach.greet();
peach.displayCrush();

*/
//# sourceMappingURL=oop.js.map