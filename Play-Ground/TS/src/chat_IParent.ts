// IParent.ts

/**
 * This file defines a TypeScript Interface.
 * An interface describes the shape of an object.
 * It does NOT generate JavaScript code after compilation.
 */
export interface IParent {
  name: string;
}